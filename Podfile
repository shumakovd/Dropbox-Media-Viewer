# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Dropbox Media Viewer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Dropbox Media Viewer
  pod 'Alamofire'
  pod 'CodableAlamofire'

  pod 'SwiftyJSON'
  pod 'SwiftyDropbox'

  # Pods for UI/UX
  pod 'Kingfisher', '~> 5.14.1'

  target 'Dropbox Media ViewerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Dropbox Media ViewerUITests' do
    # Pods for testing
  end

end
